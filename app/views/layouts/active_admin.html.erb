<!DOCTYPE html>
<html>
  <head>
    <title>Amazonia Pet Supplies</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "active_admin", "data-turbo-track": "reload" %>
    <%= javascript_pack_tag 'active_admin' %>
      </head>

  <body>
    <div id="active_admin_content">
      <%= yield %>
    </div>
    <footer>
      <nav>
        <%= link_to "Logout", destroy_admin_user_session_path, method: :delete, data: { confirm: 'Are you sure?' } %>
      </nav>
    </footer>
  </body>
</html>
